import './Actions.css';

const Actions = () => {
    return (
        <div className="actions">
            <button className="btn reset">Reset</button>
            <button className="btn autocomplete">Autocomplete</button>
            <button className="btn upload">Upload</button>
        </div>
    );
};

export default Actions; 